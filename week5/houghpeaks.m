function peaks = houghpeaks(H, numpeaks)
  # return the x & y indices of the values in H
  # that are the highest (count = numpeaks)
  
  # peaks(1, :) = x coordinates of the peak
  # peaks(2, :) = y coordinates of the peak
  # peaks(3, :) = values of the peaks
  peaks = zeros(numpeaks, 3);
  
  
end